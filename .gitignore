# Generated by `hecks-life run-boot` Phase 4 (system_prompt rendering).
# The bluebook of record is `self/system_prompt/system_prompt_assembly/`
# and per-section content lives in `system_prompt_content.fixtures`.
# Phase 1 of i145 landed those fixtures ; Phase 2/3 (i495) retires the
# Rust render and deletes `miette_prompt.md.template`.
self/system_prompt.md
